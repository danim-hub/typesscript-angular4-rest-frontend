<!-- File generated by Telosys Tools Generator ( version 3.0.0 ) - Date 2018-07-03 ( Time 19:55:37 ) -->

<div class="material-card-wide mdl-card mdl-shadow--2dp">
  <div class="mdl-card__title mdl-card--border">
    <h2 class="mdl-card__title-text">{{ title }}</h2>
  </div>
  <div class="mdl-card__supporting-text" *ngIf="!user">
    Not logged in
  </div>
  <div class="mdl-card__supporting-text" *ngIf="user">
    <img class="card-img-top img-responsive photo" src="{{ user.photoUrl }}">
    <p> {{ user.name}}</p>
    <p> {{ user.email }}</p>
    <p> {{ user.authToken }}</p>
  </div>
  <div class="mdl-card__actions mdl-card--border">
    <div class="buttonHolder">
      <a (click)="signOut()" class="mdl-button mdl-button--colored mdl-js-button mdl-js-ripple-effect" *ngIf="user">
        Logout
      </a>
      <div>
        <a (click)="signInWithFB()" class="mdl-button mdl-button--colored mdl-js-button mdl-js-ripple-effect" *ngIf="!user">
          <span class="fa fa-facebook"></span>&nbsp;Sign in with Facebook</a>
      </div>
      <div>
        <a (click)="signInWithGoogle()" class="mdl-button mdl-button--colored mdl-js-button mdl-js-ripple-effect" *ngIf="!user">
          <span class="fa fa-google"></span>&nbsp;Sign in with Google</a>
      </div>
      <div>
          <a (click)="signInWithLinkedIn()" class="mdl-button mdl-button--colored mdl-js-button mdl-js-ripple-effect" *ngIf="!user">
            <span class="fa fa-linkedin"></span>&nbsp;Sign in with Linked in</a>
        </div>
      </div>
  </div>
</div>